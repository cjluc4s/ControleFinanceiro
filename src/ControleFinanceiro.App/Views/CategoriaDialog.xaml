<ui:FluentWindow x:Class="ControleFinanceiro.App.Views.CategoriaDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        Title="Categoria" Height="260" Width="340"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        ExtendsContentIntoTitleBar="True">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <ui:TitleBar Grid.Row="0" Title="Categoria"/>

        <Grid Grid.Row="1" Margin="16">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock Text="Nome" Margin="0,0,0,4"/>
            <TextBox x:Name="TextNome" Grid.Row="0" Margin="0,20,0,12"/>

            <TextBlock Grid.Row="1" Text="Tipo" Margin="0,0,0,4"/>
            <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,0,0,12">
                <RadioButton x:Name="RadioReceita" Content="Receita" GroupName="TipoCategoria" Margin="0,0,16,0"/>
                <RadioButton x:Name="RadioDespesa" Content="Despesa" GroupName="TipoCategoria"/>
            </StackPanel>

            <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Cancelar" Width="90" Margin="0,0,8,0" Click="Cancelar_Click"/>
                <ui:Button Content="Salvar" Appearance="Primary" Width="90" IsDefault="True" Click="Salvar_Click"/>
            </StackPanel>
        </Grid>
    </Grid>
</ui:FluentWindow>
