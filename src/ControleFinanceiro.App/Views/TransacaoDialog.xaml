<ui:FluentWindow x:Class="ControleFinanceiro.App.Views.TransacaoDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        Title="Lançamento" Height="460" Width="380"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        ExtendsContentIntoTitleBar="True">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <ui:TitleBar Grid.Row="0" Title="Lançamento"/>

        <Grid Grid.Row="1" Margin="16">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock Text="Tipo" Margin="0,0,0,4"/>
            <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,12">
                <RadioButton x:Name="RadioReceita" Content="Receita" GroupName="Tipo" Margin="0,0,16,0" Checked="TipoAlterado"/>
                <RadioButton x:Name="RadioDespesa" Content="Despesa" GroupName="Tipo" Checked="TipoAlterado"/>
            </StackPanel>

            <TextBlock Grid.Row="2" Text="Descrição" Margin="0,0,0,4"/>
            <TextBox x:Name="TextDescricao" Grid.Row="2" Margin="0,20,0,12"/>

            <Grid Grid.Row="3" Margin="0,0,0,12">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="12"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0">
                    <TextBlock Text="Valor" Margin="0,0,0,4"/>
                    <TextBox x:Name="TextValor"/>
                </StackPanel>
                <StackPanel Grid.Column="2">
                    <TextBlock Text="Data" Margin="0,0,0,4"/>
                    <DatePicker x:Name="DatePickerData"/>
                </StackPanel>
            </Grid>

            <TextBlock Grid.Row="4" Text="Categoria" Margin="0,0,0,4"/>
            <ComboBox x:Name="ComboCategoria" Grid.Row="4" Margin="0,20,0,12"
                      DisplayMemberPath="Nome"/>

            <TextBlock Grid.Row="5" Text="Conta" Margin="0,0,0,4"/>
            <ComboBox x:Name="ComboConta" Grid.Row="5" Margin="0,20,0,12"
                      DisplayMemberPath="Nome"/>

            <StackPanel Grid.Row="7" Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Cancelar" Width="90" Margin="0,0,8,0" Click="Cancelar_Click"/>
                <ui:Button Content="Salvar" Appearance="Primary" Width="90" IsDefault="True" Click="Salvar_Click"/>
            </StackPanel>
        </Grid>
    </Grid>
</ui:FluentWindow>
